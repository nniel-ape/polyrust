target/
*.db
*.db-journal
.env
config.toml

# Docker volumes and overrides
data/
docker-compose.override.yml

# SOPS deploy key (never commit private keys)
.sops/deploy-key.txt

# ralphex progress logs
progress*.txt
.DS_Store
backtest_output.txt
sweep_results*
